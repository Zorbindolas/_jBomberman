package view;
/**
 * This type of objects implements an operation to increase Decorator's footage
 */
public interface Animate {
	/**
	 * The algorithm to increase Decorator's footage.
	 * @param g the to be increased Decorator
	 */
	void animate(Object g);	
}
